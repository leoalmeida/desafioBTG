name: ms-order Module CI Pipeline

on:
  push:
    branches:
      - main

permissions:
  contents: read
  id-token: write

jobs:
  code-quality-orders:
    name: ms-order Linting and Code Quality Analysis
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Set up JDK 17
        uses: actions/setup-java@v5
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: 'maven'
      
      - name: Run spotless check
        run: mvn -B -f ms-order/pom.xml spotless:check

      - name: Run Checkstyle
        run: mvn -B -f ms-order/pom.xml checkstyle:check

      - name: Generate Checkstyle report
        run: mvn -B -f ms-order/pom.xml checkstyle:checkstyle

      - name: Run PMD check
        run: mvn -B -f ms-order/pom.xml pmd:check

      - name: generate PMD report
        run: mvn -B -f ms-order/pom.xml pmd:pmd
      
      - name: Run spotbugs check
        run: mvn -B -f ms-order/pom.xml spotbugs:check

      - name: Generate SpotBugs report
        run: mvn -B -f ms-order/pom.xml spotbugs:spotbugs
  
  test-orders:
    needs: [code-quality-orders]
    name: ms-order Module Automated Tests
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Set up JDK 17
        uses: actions/setup-java@v5
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: 'maven'
  
      - name: Roda os testes do módulo ms-order
        run: mvn -B -f ms-order/pom.xml clean package

      - name: Run E2E tests do módulo ms-order
        run: mvn -B -f ms-order/pom.xml -Ddocker.e2e=true -Dtest=OrderFlowE2ETest test

      - name: Run Code Quality Analysis do módulo ms-order
        run: mvn -B -f ms-order/pom.xml jacoco:report

      - name: Upload coverage to Codecov
        uses: codecov/codecov-action@v5
        with:
          use_oidc: true
          files: ms-order/target/site/jacoco/jacoco.xml
          flags: ms-order
          name: ms-order-coverage
          fail_ci_if_error: false
          verbose: true

      - name: Upload Coverage Report do módulo ms-order
        uses: actions/upload-artifact@v6
        with:
          name: coverage-report
          path: ms-order/target/site/jacoco/
          retention-days: 1

  build-orders:
    needs: [test-orders]
    name: ms-order Module Build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - name: Set up JDK 17
        uses: actions/setup-java@v5
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: 'maven'
      - name: Build ms-order module
        run: mvn -B -f ms-order/pom.xml clean package -DskipTests
      
      - name: Upload Backend Artifact
        uses: actions/upload-artifact@v6
        with:
          name: ms-order-jar
          path: ms-order/target/*.jar
          retention-days: 1

  #zap-scan:
  #  needs: build-project
  #  uses: ./.github/workflows/zapscan.yml
  #  secrets: inherit
  
  #docker:
  #  needs: zap-scan
  #  uses: ./.github/workflows/docker.yml
  #  secrets: inherit
  
  #deploy_google:
  #  needs: docker
  #  uses: ./.github/workflows/release-gcloud.yml
  #  secrets: inherit